{
  "scripts": {
    "install":  "npm install --prefix backend && npm install --prefix frontend && npm install --prefix couper",
    "couper":   "npm start --prefix couper",
    "backend":  "npm start --prefix backend",
    "frontend": "npm start --prefix frontend",
    "start":    "npm run couper & npm run backend & npm run frontend && fg"
  }
}
